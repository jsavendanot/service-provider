import React from 'react';

export type BackgroundProps = {};
export const Background: React.FC<BackgroundProps> = ({}) => {
  console.log({});
  return <div>Cultural background</div>;
};

export default Background;
